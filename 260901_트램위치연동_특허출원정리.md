# 트램 위치 품질 연동형 가변 구역 위험경보 — 특허 출원 정리

- 작성일: 2026-09-01
- 검토 대상: `trem_detection` 구현
- 출원 방향: 현재 구현에 기반한 국내 특허 1건

## 1. 출원 요약

### 발명 명칭안

> 트램 위치의 불확실도·유효성 및 영상 객체 점유구역의 방향별 구역 간격에 기반한 위험경보 방법 및 시스템

### 발명 요지

카메라별 감시영역을 복수의 다각형 구역으로 구분하고 각 구역에 위험등급과 구역 식별자를 설정한다. 영상에서 검출된 사람 객체의 바운딩박스 하단 중앙점을 지면 접촉 위치로 산정하고, 해당 위치가 포함된 객체 점유구역을 결정한다. 트램 위치의 불확실도에 따라 복수의 트램 후보구역을 생성하고, 진행 방향에 따라 전방·후방 기준을 구분하여 객체 점유구역과의 최소 구역 간격을 산정한다. 위치정보가 오래되었거나 명시적으로 누락된 경우에는 위험·경고 구역의 사람 객체에 보수적 경보를 적용한다.

### 출원 판단

현재 구현만으로 출원서 작성은 가능하다. 다만 사람의 지면 접촉 위치를 이용한 금지구역 판정과 이동체 위치 기반 경보에는 선행기술이 존재하므로, 독립청구항은 다음 구성의 결합으로 한정한다.

- 카메라별 복수 다각형 구역
- 구역별 위험등급과 순차적 구역 식별자
- 객체의 지면 접촉 위치에 따른 점유구역 결정
- 트램 구역과 객체 점유구역 사이의 이산적 구역 간격
- 트램 위치 불확실도에 따른 복수 후보구역과 최소 구역 간격
- 트램 진행 방향에 따른 전방·후방의 비대칭 임계값
- 위치정보의 경과시간 또는 누락에 따른 보수적 경보
- 위험등급과 위치 품질을 동시에 이용한 선택적 경보

### 전체 후보 우선순위

| 순위 | 출원 후보 | 구현 상태 | 출원 방향 |
|---:|---|---|---|
| 1 | 트램 위치 품질 연동형 가변 구역 위험경보 | 구현됨 | 1차 출원 |
| 2 | 교통약자 유형 기반 선제 위험경보 | 부분 구현 | 1차 출원의 종속항 또는 후속 출원 |
| 3 | 밀도추정·객체검출 결합형 혼잡 위험판정 | 기반 구현 | 추가 구현·시험 후 출원 |
| 4 | 위험도 기반 적응형 다중 AI 처리주기 | 부분 구현 | 독립 출원 제외, 종속항 검토 |

## 2. 해결 과제

고정 위험구역 방식은 트램이 해당 구역에서 멀리 떨어져 있는 경우에도 사람의 진입만으로 경보를 생성할 수 있다. 이로 인해 현장 운영과 직접 관련되지 않는 경보가 증가할 수 있다.

본 발명은 트램의 현재 위치와 사람의 점유구역을 동일한 구역 식별자 체계로 표현하고, 위치정보의 불확실도·경과시간·진행 방향을 위험등급과 함께 판정한다. 이를 통해 위치 오차가 있는 경우에는 경보 범위를 보수적으로 확대하고, 위치정보가 오래되었거나 누락된 경우에는 안전모드 경보를 생성한다.

## 3. 구현 구성

| 구성 | 처리 내용 | 상태 | 코드 위치 |
|---|---|---|---|
| 구역 설정 | 카메라별 다각형 구역에 `danger`, `warning`, `safe` 및 `zoneIndex` 설정 | 구현됨 | `camera_zones.py` |
| 좌표 보정 | 기준 영상과 입력 영상의 해상도 비율에 따라 구역 좌표 보정 | 구현됨 | `frame_objects.py` |
| 객체 검출 | 영상에서 사람 등 대상 객체와 바운딩박스 검출 | 구현됨 | `frame_objects.py` |
| 지면 위치 | 바운딩박스 하단 중앙점을 객체의 지면 접촉 위치로 산정 | 구현됨 | `frame_objects.py` |
| 점유구역 | 지면 접촉 위치를 포함하는 다각형 구역 결정 | 구현됨 | `frame_objects.py` |
| 중첩 처리 | 복수 구역에 포함되면 위험등급이 높은 구역 선택 | 구현됨 | `frame_objects.py` |
| 트램 위치 입력 | 분석 요청의 `tramZone`을 트램 구역 식별자로 수신 | 구현됨 | `crowd_analysis_protocol.py` |
| 위치 불확실도 | `tramZoneUncertainty` 범위의 복수 후보구역 생성 | 구현됨 | `frame_objects.py` |
| 방향별 기준 | `tramDirection`과 전방·후방 임계값에 따라 적용 기준 선택 | 구현됨 | `frame_objects.py` |
| 위치 유효성 | `tramObservedAt`과 최대 허용 경과시간을 비교하여 오래된 위치 판정 | 구현됨 | `crowd_analysis_protocol.py` |
| 누락 안전모드 | 명시적으로 위치 누락 안전모드를 요청하면 위험·경고 구역의 사람에게 경보 | 구현됨 | `frame_objects.py` |
| 구역 간격 | 객체 구역과 복수 트램 후보구역 사이의 최소 구역 간격 산정 | 구현됨 | `frame_objects.py` |
| 경보 생성 | 사람·위험등급·위치 품질·방향별 구역 간격 조건에 따라 경보 설정 | 구현됨 | `frame_objects.py` |
| 결과 집계 | 경보 객체 수와 전체 경보등급 산출 | 구현됨 | `crowd_analysis_protocol.py` |

### 분석 요청 예시

```json
{
  "tramZone": 6,
  "tramDirection": 1,
  "tramZoneUncertainty": 1,
  "tramObservedAt": "2026-09-01T14:30:00+09:00",
  "tramPositionMaxAgeSeconds": 2.5,
  "forwardZoneGapThreshold": 3,
  "rearZoneGapThreshold": 1,
  "failSafeOnMissingTramPosition": true
}
```

`tramDirection`의 `1`은 구역 식별자가 증가하는 방향, `-1`은 감소하는 방향, `0`은 방향 미적용을 의미한다. `tramZoneUncertainty`는 0~100 범위의 구역 수이며, 값이 1이면 `tramZone` 전후 1개 구역을 후보에 포함한다. 기존 요청처럼 신규 필드를 생략하면 기존의 대칭 구역 간격 판정을 유지한다.

## 4. 처리 절차

1. 카메라 영상과 트램의 현재 구역 식별자 `tramZone`을 수신한다.
2. 카메라별 구역정보를 불러오고 입력 영상 해상도에 맞게 다각형 좌표를 보정한다.
3. 입력 영상에서 사람 객체를 검출한다.
4. 사람 객체의 바운딩박스 하단 중앙점을 지면 접촉 위치로 산정한다.
5. 지면 접촉 위치가 포함된 객체 점유구역을 결정한다.
6. 복수 구역이 중첩된 경우 위험등급이 높은 구역을 선택한다.
7. `tramZoneUncertainty`에 따라 복수의 트램 후보구역을 생성한다.
8. 트램 진행 방향을 기준으로 객체 점유구역이 전방인지 후방인지 판정한다.
9. 객체 점유구역과 후보구역 사이의 최소 구역 간격을 산정하고 전방·후방 임계값 중 하나를 적용한다.
10. `tramObservedAt` 이후의 경과시간이 허용값을 초과하면 위치정보를 유효하지 않은 상태로 판정한다.
11. 객체가 사람이고 점유구역이 위험 또는 경고이며 방향별 구역 간격이 기준 이하이면 경보를 생성한다.
12. 위치정보가 오래되었거나 명시적으로 누락된 경우 위험·경고 구역의 사람에게 보수적 경보를 생성한다.
13. 경보 객체 수, 가장 높은 경보등급 및 경보 사유를 분석 결과에 포함한다.

## 5. 독립청구항 구성안

> **청구항 1.** 영상 기반 위험경보 방법에 있어서,  
> 트램의 현재 위치에 대응하는 트램 구역 식별자를 수신하는 단계;  
> 카메라 영상에서 사람 객체를 검출하는 단계;  
> 상기 사람 객체의 바운딩박스 하단 중앙점을 지면 접촉 위치로 산정하는 단계;  
> 위험등급 및 구역 식별자가 설정된 복수의 다각형 구역 중 상기 지면 접촉 위치를 포함하는 객체 점유구역을 결정하는 단계;  
> 상기 트램 위치정보의 불확실도에 따라 상기 트램 구역 식별자를 포함하는 복수의 트램 후보구역을 결정하는 단계;  
> 상기 트램의 진행 방향을 기준으로 상기 객체 점유구역의 방향을 결정하고, 상기 객체 점유구역과 상기 복수의 트램 후보구역 사이의 최소 구역 간격을 산정하는 단계;  
> 상기 트램 위치정보의 관측시각과 현재시각의 차이에 따라 상기 트램 위치정보의 유효성을 판정하는 단계; 및  
> 상기 트램 위치정보가 유효한 경우에는 상기 객체 점유구역의 위험등급 및 방향별 기준 간격에 따라 경보를 생성하고, 상기 트램 위치정보가 유효하지 않은 경우에는 상기 객체 점유구역의 위험등급에 따라 보수적 경보를 생성하는 단계를 포함하는 영상 기반 위험경보 방법.

위 문안은 발명 구성을 설명하기 위한 초안이다. 최종 청구범위는 정식 선행기술조사와 변리사 검토를 거쳐 확정한다.

## 6. 종속청구항 구성안

1. 기준 영상 해상도와 입력 영상 해상도의 비율에 따라 다각형 구역의 좌표를 보정하는 구성
2. 지면 접촉 위치가 복수 구역에 포함되는 경우 `danger`, `warning`, `safe` 순으로 객체 점유구역을 결정하는 구성
3. 트램 위치의 불확실도 범위에 포함되는 연속 구역을 복수의 트램 후보구역으로 결정하는 구성
4. 객체 점유구역과 복수의 트램 후보구역 사이의 절댓값 차이 중 최솟값을 구역 간격으로 산정하는 구성
5. 트램 진행 방향의 전방과 후방에 서로 다른 구역 간격 임계값을 적용하는 구성
6. 트램 위치정보의 관측시각 이후 경과시간이 최대 허용시간을 초과하면 위치정보를 오래된 상태로 판정하는 구성
7. 위치정보가 오래되었거나 명시적으로 누락된 경우 위험 또는 경고 구역의 사람에게 보수적 경보를 생성하는 구성
8. 경보 결과에 `TRAM_ZONE_PROXIMITY`, `TRAM_POSITION_UNCERTAIN`, `TRAM_POSITION_STALE` 또는 `TRAM_POSITION_MISSING` 중 하나의 경보 사유를 포함하는 구성
9. 검출 객체가 사람인 경우에만 트램 위치 연동 경보를 생성하는 구성
10. 객체 점유구역에 구역 식별자가 설정되지 않은 경우 해당 객체를 경보 대상에서 제외하는 구성
11. 카메라별 저장 구역정보를 분석 요청에 포함된 구역정보보다 우선 적용하는 구성
12. 경보 객체의 위험등급 중 가장 높은 등급을 전체 경보등급으로 결정하는 구성
13. 화면 세로 위치별 성인 기준 신장과 사람 바운딩박스 높이의 비율을 이용하여 사람 유형을 추정하는 구성
14. 사람 객체 주변에서 검출된 이동보조기기를 해당 사람 객체와 연관하고 그 결과를 경보정보에 포함하는 구성

13항과 14항은 현재 구현된 사람 유형 추정 결과를 경보정보에 포함하는 범위로 한정한다. 사람 유형에 따라 경보 시점을 변경하는 선제 경보는 현재 구현 범위에 포함되지 않는다.

## 7. 도면 구성

| 도면 | 내용 |
|---:|---|
| 도 1 | CCTV, AI 분석 서버, 트램 위치 제공 시스템 및 경보 수신 시스템의 구성도 |
| 도 2 | 승강장의 복수 다각형 구역, 위험등급 및 `zoneIndex` 배치도 |
| 도 3 | `tramZone`과 `tramZoneUncertainty`에 따른 복수 후보구역의 변화 예시 |
| 도 4 | 사람 바운딩박스와 하단 중앙점의 산정 예시 |
| 도 5 | 중첩 구역에서 위험등급이 높은 구역을 선택하는 예시 |
| 도 6 | 트램 위치 수신부터 경보 생성까지의 처리 순서도 |
| 도 7 | 진행 방향의 전방·후방에 서로 다른 구역 간격을 적용하는 사례 |
| 도 8 | 위치정보의 정상·불확실·오래됨·누락 상태별 경보 전환 사례 |
| 도 9 | 사람 유형 및 이동보조기기 연관 결과를 포함한 경보정보 예시 |

## 8. 출원 제외 범위

다음 구성은 현재 구현되지 않았으므로 이번 출원의 핵심 청구범위에서 제외한다.

- 트램 속도·제동거리에 따른 구역 범위 조정
- 객체 추적 식별자에 따른 경보 유지 및 해제 히스테리시스
- 구역 연결 그래프의 최단거리 산정
- 교통약자 유형별 경보 시점 또는 임계값 조정
- 밀도맵과 객체검출 인원 차이에 따른 위험도 보정
- 위험도에 따른 AI 모델별 실행주기 변경

위 구성은 구체적인 계산 절차와 실시 가능성을 확보한 이후 후속 출원 또는 국내우선권 주장 출원의 보강 대상으로 검토한다.

## 9. 출원 준비자료

- 카메라별 구역 배치도와 `zoneIndex` 관계도
- `tramZone` 변화에 따른 경보·비경보 사례
- 위치 불확실도별 후보구역 및 최소 구역 간격 사례
- 진행 방향별 전방·후방 임계값 사례
- 위치정보 경과시간 및 누락 상태별 보수적 경보 사례
- 구역 중첩 시 위험등급 선택 사례
- 입력 영상 해상도 변경에 따른 좌표 보정 사례
- 분석 요청 및 경보 결과 JSON 예시
- 관련 소스코드와 시험 결과
- 최초 구현일을 확인할 수 있는 Git 이력
- 설계 회의록과 발명자별 기술적 기여 내용
- 외부 시연·제안서·영상·공개 저장소 게시 이력

현재 트램 연동 구현은 작업트리의 미커밋 변경에 포함되어 있다. 최초 구현일과 발명 내용을 입증할 수 있도록 소스코드, 시험 결과 및 설계자료의 이력을 확정할 필요가 있다.

## 10. 선행기술 검토 대상

- [US20060093187A1 — Video-based encroachment detection](https://patents.google.com/patent/US20060093187A1/en): 객체의 지면 접촉 위치와 금지구역을 이용한 경보
- [DE102018206593A1 — Mobile recognition of passengers and objects on platforms](https://patents.google.com/patent/DE102018206593A1/en): 열차 속도에 따른 승강장 가상 위험선 조정
- [WO2006022594A1 — Multi-sensor intrusion detection system](https://patents.google.com/patent/WO2006022594A1/en): 열차 위치 센서와 CCTV를 결합한 철도 침입 감시
- [US7460686B2 — Security monitor device at station platform](https://patents.google.com/patent/US7460686B2/en): 승강장 영상, 위치 및 이동 상태를 이용한 안전 감시
- [WO2022040610A1 — Node-based near-miss detection](https://patents.google.com/patent/WO2022040610A1): 객체 속도·크기·예측 궤적 등에 따른 동적 위험구역
- [WO2024177948A1 — Video analytics for industrial floor setting](https://patents.google.com/patent/WO2024177948A1/en): 사람의 발 위치와 이동체 주변 위험영역을 이용한 위험 판정
- [US20250285443A1 — Monitoring device for vehicle exclusion zone](https://patents.google.com/patent/US20250285443A1/en): 차량 출입구 주변 배제구역의 사람 검출 및 경보

정식 조사에서는 각 선행문헌의 독립청구항과 본 출원 구성의 차이를 청구항 단위로 대비한다. 특히 순차적 구역 식별자, 트램 구역과 객체 점유구역의 이산적 간격 및 위험등급의 결합이 하나의 선행문헌에 모두 공개되어 있는지 확인해야 한다.

## 11. 출원 권고

현재 구현을 기준으로 국내 특허 1건을 우선 출원한다. 독립청구항은 트램 위치의 불확실도·유효성·진행 방향, 객체의 지면 접촉 위치, 복수 후보구역과의 최소 구역 간격 및 위치정보 이상 시 보수적 경보의 결합으로 구성한다. 사람 유형과 이동보조기기 연관 기능은 종속청구항과 실시예에 포함한다.

본 자료는 기술 구성 정리안이며 특허 등록 가능성에 관한 법률 의견을 대체하지 않는다. 최종 명세서와 청구범위는 정식 선행기술조사 및 변리사 검토를 거쳐 확정한다.

## 12. 후속 출원 후보

### 12.1 교통약자 유형 기반 선제 위험경보

#### 기술 개요

단안 CCTV에서 지면 위치별 기준 신장을 이용하여 사람의 유형을 추정하고, 휠체어·스쿠터 등 이동보조기기 검출 결과를 사람 객체와 연관한다. 객체의 이동 이력으로 위험구역 진입을 예측한 뒤, 추정된 교통약자 유형에 따라 경보 시점 또는 경보 수준을 조정한다.

#### 구현 상태

- 화면 세로 위치별 성인 기준 신장 보정: 구현됨
- 성인·어린이·불확실 유형 추정: 구현됨
- 사람 주변의 이동보조기기 검출 결과 연관: 구현됨
- 객체 이동 이력 기반 미래 위험구역 진입 예측: 구현됨
- 연속 프레임 확인과 반복 경보 억제: 구현됨
- 교통약자 유형별 경보 시점 또는 임계값 조정: 미구현

#### 출원 방향

현재 구현은 사람 유형과 이동보조기기 정보를 경보 결과에 포함하는 수준이므로 1차 출원의 종속청구항과 실시예에 포함한다. 유형별 최소 대피시간과 경보 여유시간을 구현하고 운영 영상으로 검증한 이후 독립 출원 여부를 결정한다.

### 12.2 밀도추정·객체검출 결합형 혼잡 위험판정

#### 기술 개요

밀도추정 모델의 밀도맵을 위험구역별로 적분하고 개별 객체검출 결과와 비교한다. 밀도 기반 추정 인원과 검출 객체 수의 차이를 가림 또는 검출 누락의 지표로 사용하여 위험도를 보정한다.

#### 구현 상태

- DM-Count 기반 전역 인원 추정: 구현됨
- YOLO 기반 개별 객체검출: 구현됨
- 카메라별 위험구역 판정: 구현됨
- 구역별 밀도맵 적분: 미구현
- 밀도추정·객체검출 불일치 보정: 미구현

#### 출원 방향

구역별 밀도 적분, 불일치의 시간적 지속 판정 및 트램 인접 구역의 위험도 상향을 구현한 이후 출원 여부를 재검토한다. 밀도맵과 객체검출의 결합 자체에는 선행기술이 존재하므로 트램 정차예측구간의 검출 불확실성을 보정하는 구체적 제어로 한정할 필요가 있다.

### 12.3 위험도 기반 적응형 다중 AI 처리주기

#### 기술 개요

얼굴 검출, 객체검출, 군중 계수 및 이동보조기기 검출 모델의 실행주기를 위험 상태에 따라 독립적으로 조정한다. 개인정보 비식별화 수준은 유지하면서 위험도가 높아질 때 안전 분석 모델의 처리주기를 단축한다.

#### 구현 상태

- 객체검출과 얼굴 검출의 프레임별 실행: 구현됨
- 군중 계수의 고정 주기 실행: 구현됨
- 이동보조기기 검출의 고정 주기 실행: 구현됨
- 위험도에 따른 동적 실행주기 변경: 미구현

#### 출원 방향

일반적인 연산 자원 스케줄링과의 차별성이 제한되므로 독립 출원에서 제외한다. 개인정보 비식별화 처리를 유지하면서 안전 분석 모델의 실행주기만 변경하는 구성은 1차 또는 혼잡 위험판정 출원의 종속청구항으로 검토한다.

## 13. 단독 출원 우선순위가 낮은 구현

다음 구현은 제품 구성요소로 활용하되 독립청구항보다 1차 또는 후속 출원의 세부 구성으로 포함한다.

- 바운딩박스 하단 중앙점을 이용한 ROI 포함 판정
- 입력 영상 해상도에 따른 다각형 좌표 비례 보정
- 위험·주의·안전 다각형 설정 UI
- 일정 프레임 동안 얼굴 검출 영역을 유지하는 블러 보정
- 복수의 공개 AI 모델을 하나의 분석 과정에서 실행하는 구성
- 공유 파일 경로에서 이미지를 읽고 분석 결과만 반환하는 서버 구성

## 14. 출원 포트폴리오

### 14.1 1차 출원

트램 위치 품질 연동형 가변 구역 위험경보를 중심으로 출원한다.

- 독립항: 트램 위치 불확실도·유효성·진행 방향, 객체 지면 접촉 위치, 복수 후보구역과의 최소 구역 간격 및 보수적 경보의 결합
- 종속항: 해상도 보정, 중첩 구역 우선순위, 경보 사유, 사람 유형 및 이동보조기기 정보
- 시스템항: 카메라, 객체검출부, 구역 판정부, 트램 위치 수신부 및 경보 생성부
- 기록매체항: 위 방법을 수행하는 프로그램

### 14.2 후속 출원

교통약자 유형 기반 선제 위험경보는 유형별 경보 여유시간을 구현한 이후 분할 또는 별도 출원을 검토한다. 밀도추정·객체검출 결합형 혼잡 위험판정은 구역별 밀도 적분과 운영 영상 평가를 완료한 이후 후속 출원 대상으로 검토한다.

EOD
